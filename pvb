
-- if game.PlaceId == 127742093697776 then
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = " ",
   Icon = 0, 
   LoadingTitle = " ",
   LoadingSubtitle = " ",
   ShowText = " ",
   Theme = "DarkBlue",

   ToggleUIKeybind = "G", 

   DisableRayfieldPrompts = true,
   DisableBuildWarnings = true,

   ConfigurationSaving = {
      Enabled = true,
      FolderName = "Chu",
      FileName = "Chu"
   },

   Discord = {
      Enabled = false, 
      Invite = "dc.gg/mandarynkahub", 
      RememberJoins = true 
   },

   KeySystem = false, 
   KeySettings = {
      Title = "Join discord to get free key !",
      Subtitle = "Key System",
      Note = "KEY: MandarynkaKey301 | To get free key join to dc.gg/mandarynkahub !",
      FileName = "Key", 
      SaveKey = true,
      GrabKeyFromSite = true,
      Key = {"https://pastebin.com/raw/nnSbLYF0"} 
   }
})

local MainTab = Window:CreateTab("🏠 Main", nil)

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local AutoBuyEnabled = false
local SelectedSeeds = {
    --"Cactus Seed", 
    --"Strawberry Seed", 
    --"Pumpkin Seed", 
    --"Sunflower Seed", 
    --"Dragon Fruit Seed", 
    --"Eggplant Seed", 
    --"Watermelon Seed", 
    --"Grape Seed", 
    --"Cocotank Seed", 
    --"Carnivorous Plant Seed", 
    "Mr Carrot Seed", 
    "Tomatrio Seed", 
    "Shroombino Seed", 
    "Mango Seed", 
    "King Limone Seed",
    "Starfruit Seed"
	}
local SelectedGears = {"Water Bucket"}

local function ensureTable(value)
	if typeof(value) == "table" then
		return value
	elseif typeof(value) == "string" then
		return { value }
	else
		return {}
	end
end

MainTab:CreateToggle({
	Name = "Auto Buy Enabled",
	CurrentValue = false,
	Flag = "AutoBuyEnabled",
	Callback = function(Value)
		AutoBuyEnabled = Value
            Rayfield:Notify({
   Title = "Succesly change value",
   Content = "Succesly change autobuy",
   Duration = 5,
   Image = nil,
})
	end,
})

local Divider = MainTab:CreateDivider()


task.spawn(function()
	while true do
		if AutoBuyEnabled then
			for _, seed in ipairs(SelectedSeeds) do
				pcall(function()
					ReplicatedStorage.Remotes.BuyItem:FireServer(seed)
				end)
				task.wait(0.3)
			end

			for _, gear in ipairs(SelectedGears) do
				pcall(function()
					ReplicatedStorage.Remotes.BuyGear:FireServer(gear)
				end)
				task.wait(0.3)
			end
		end
		task.wait(0.1)
	end
end)
